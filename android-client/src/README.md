# Android Source Layout

Greenfield Android project source. التقسيم المستهدف:

- `app`: composition root،navigation وapplication lifecycle.
- `core`: contracts،networking،storage،security وdesign system.
- `features`: pairing،capture،invoice review،catalog search/create وcommit status.

لا يوجد Gemini أوSQL module داخل Android.
