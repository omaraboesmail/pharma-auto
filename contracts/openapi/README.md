# OpenAPI Contracts

توضع OpenAPI 3.1 documents هنا بعد مراجعة resource/state model. Generated clients تأتي من هذه الملفات،ولا تُكتب DTOs متعارضة يدويًا.

`local-connector.v1.json` initializes the Android commercial-edit preview boundary. It is explicitly read-only and exposes no Genius identifiers or write endpoint.
