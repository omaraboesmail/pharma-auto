# Contract Examples

يحتوي synthetic examples بلا بيانات صيدلية حقيقية. لكل schema: happy path،expiry splits،New Item،missing number،duplicate،validation failure وversion mismatch.

`invoice-revision.v1.example.json` covers EGP،two sequential percentage discounts and a tax-inclusive per-box selling price for new stock only.
